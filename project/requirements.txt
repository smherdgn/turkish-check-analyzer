
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5
opencv-python-headless>=4.5.0
Pillow>=9.0.0
pytesseract>=0.3.8
easyocr>=1.7.0
paddleocr>=2.6
requests>=2.25.0
numpy>=1.20.0
aiohttp>=3.8.0
# For EasyOCR, you might need to install torch and torchvision separately if issues arise,
# though easyocr usually pulls them in.
# e.g., torch torchvision torchaudio
# Refer to EasyOCR documentation for specific PyTorch version compatibility if needed.
